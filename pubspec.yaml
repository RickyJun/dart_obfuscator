name: dart_obfuscator
description: A starting point for Dart libraries or applications.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  analyzer: ^12.1.0 # Or the version matching Flutter's Dart SDK
  args: ^2.7.0
  path: ^1.9.1
  glob: ^2.1.3
  yaml: ^3.1.3
